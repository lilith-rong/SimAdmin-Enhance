# SimAdmin 真机测试清单（多 SIM / 多读卡器）

本轮改动把项目从"单基带 + 全局状态"改成"每条线路(line)各自独立"。
绝大多数逻辑无法在开发机验证，必须在真机上跑。本文件按功能分组列出
**需要真机验证的项**，每项写明：怎么测、期望结果、失败时看哪里。

- 设备：使用测试环境自己的地址和管理员凭据；不要把密码写入仓库。
- 前置：至少插 **两张不同运营商的 SIM**（最好一张国内、一张海外），
  两个基带都能被 ModemManager 识别（`mmcli -L` 应看到 ≥2 个 modem）
- 日志：`journalctl -u simadmin -f`，前端「系统事件」页，
  以及 `/api/vowifi/lines/{line_id}/events`、`/api/volte/lines/{line_id}`

---

## 0. 环境准备（每次刷版本后先做一遍）

| # | 步骤 | 期望结果 |
|---|------|----------|
| 0.1 | `mmcli -L` | 列出 ≥2 个 modem，且 `mmcli -m <n>` 能读到各自 ICCID/IMSI |
| 0.2 | `ls -l /dev/wwan*` | 每个基带至少有 `qmi0`；装了自定义内核模块后应额外出现 `qmi1`（DATA6） |
| 0.3 | `simadmin secondary-qmi-init --dry-run` | 打印将要绑定的 `DATA6_CNTL/DATA7_CNTL` 通道与对应 remoteproc，不报错 |
| 0.4 | 打开前端「SIM 卡」页 | 两条线路都出现，`line_id` 不同，卡槽标签不重复 |

> 0.2 如果只有 `qmi0`，先检查目标内核的多 RPMSG WWAN 控制端点支持，再核对
> `deploy/system/simadmin-secondary-qmi.service`、udev 规则和 `journalctl -u simadmin-secondary-qmi`。

---

## 1. 每卡独立设置（本轮最大改动，优先测）

这一组以前**全部会打到第一个基带上**，UI 却显示在操作第 N 条线路。

| # | 操作 | 期望结果 | 失败特征 |
|---|------|----------|----------|
| 1.1 | 线路详情 → 线路B → 「小区」页，点刷新 | 显示的是 **B 的驻网小区**（PCI/EARFCN 与 A 不同） | 两条线路显示完全相同的小区列表 |
| 1.2 | 在线路B 锁一个小区 | 只有 B 被锁；切回 A 的「小区」页显示未锁定 | A 也显示成锁定 |
| 1.3 | 线路B 切换射频模式为「仅 LTE」 | `mmcli -m <B> ` 的 CurrentModes 变化；A 不变 | A 的模式跟着变了 |
| 1.4 | 线路B 应用频段锁定 | 只影响 B 的 CurrentBands | A 的频段也被改 |
| 1.5 | 线路B 「运营商」页 → 扫描 | 扫到的是 B 卡所在国家/运营商列表 | 扫到 A 卡的运营商 |
| 1.6 | 线路B 手动注册到某 PLMN | 只有 B 重新注册（A 保持在线，不掉网） | A 掉网/重注册 |
| 1.7 | 线路B 「APN」页设置 APN | 保存后重进仍是 B 的值；A 的 APN 不变 | A 的 APN 被覆盖 |
| 1.8 | 两条线路都设 APN，重启 simadmin | 各自的值都保留 | 只剩一个值 / 互相覆盖 |

**验证点（后端）**：这些请求现在都带 `line_id`。抓包或看日志确认
请求 URL 上有 `?line_id=...` 或 body 里有 `line_id`。如果没有，说明前端
某处还在走旧调用。

---

## 2. 飞行模式 / 漫游 / 数据（线路隔离）

| # | 操作 | 期望结果 |
|---|------|----------|
| 2.1 | 在线路A 开飞行模式，再切换到线路B | A 显示为开，B 保持自己的状态；刷新页面后不串线 |
| 2.2 | 分别给 A/B 设置不同的漫游策略并重启服务 | 两条线路各自恢复保存值，不存在全局值覆盖 |
| 2.3 | 线路B 单独开飞行模式 | 只有 B 的射频关闭；A 仍能收发短信 |
| 2.4 | 线路B 开飞行模式后尝试开 B 的数据 | 返回 `line_airplane_mode_enabled`，不允许 |
| 2.5 | 线路B 开数据连接 | **不产生系统默认路由**（`ip route` 的 default 不变） |
| 2.6 | 通过 B 的代理端口访问外网 | 走 B 的流量（运营商侧流量增加），主机默认路由仍走原来的出口 |

> 2.5/2.6 是"数据连接仅代理出口"的核心，必须确认 `ip route show default`
> 在开启线路数据前后完全一致。

---

## 3. VoLTE（每卡独立 IMS bearer）

| # | 操作 | 期望结果 | 失败特征 |
|---|------|----------|----------|
| 3.1 | 只开线路A 的 VoLTE | A 进入 registered；`/api/volte/lines/{A}` 里 `secondary_qmi_device` 指向 A 的 qmi1 | 报 `interface-in-use-config-match` |
| 3.2 | 保持 A 连着，再开线路B 的 VoLTE | **两条线路同时 registered**，各自 bearer 不同 | B 起来后 A 掉线 / 两个都失败 |
| 3.3 | 看 `/api/volte/lines/{line_id}` 的 attempt 列表 | 每次尝试都带 `at_cid`/`qmi_device`/`bearer_path`/`interface`/`pcscf` | 字段缺失 |
| 3.4 | 默认双栈请求，网络只给 IPv4 | 自动回落到 IPv4，日志有 `NetworkForcedIpv4` 类的分类原因 | 直接失败，不重试 |
| 3.5 | 网络明确要求 IPv6 | 直接切 IPv6，不再盲试 IPv4 | 仍按固定顺序试 |
| 3.6 | 用 A 卡收一条短信 | 短信记录的 `line_id` 是 A | 记到了 B 名下 |
| 3.7 | A、B 同运营商、P-CSCF 地址相同时同时开 VoLTE | 两条线路仍通过各自 source rule 和独立路由表到达 P-CSCF | 第二条线路走错 netdev |

---

## 4. VoWiFi（每卡独立 ePDG / TUN / 代理）

| # | 操作 | 期望结果 | 失败特征 |
|---|------|----------|----------|
| 4.1 | 只开线路A 的 VoWiFi | 建立一个 TUN 设备，名字形如 `sa_vwf_<A>` | 名字仍是固定的 `sa_vwf0` |
| 4.2 | 保持 A 连着，再开 B 的 VoWiFi | **出现第二个 TUN 设备**，两条都 registered | B 起来后 A 的 TUN 被顶掉 |
| 4.3 | `ip link` 查看 | 两个 TUN 名字不同、都 UP、各自有运营商下发的内网地址 | 只有一个 |
| 4.4 | 给 A、B 配**不同的 SOCKS5 代理**（如两个本地 mihomo 出口） | 各自的 IKE/NAT-T 走各自代理；两国 SIM 可同时在线 | 都走同一个代理 |
| 4.5 | 只给 B 配代理，A 不配 | A 直连 ePDG，B 走代理；A 的 DNS 用指定 DNS 服务器直连，B 的 DNS 走代理 | A 也被拉去走代理 |
| 4.6 | 代理填错（端口不通） | 该线路报错并回落，**不影响另一条线路** | 两条都断 |
| 4.7 | 用 B 卡发一条 IMS 短信 | 走 B 的 IMS 通道；EAP-AKA 用的是 B 的读卡器 | 认证用了 A 的卡（网络侧拒绝） |
| 4.8 | 用 B 卡拨一次 VoWiFi 电话 | INVITE 走 B 的 P-CSCF | 走了 A 的 |
| 4.9 | 关掉 B 的 VoWiFi | 只有 B 的 TUN 和 IMS 会话被清；A 完全不受影响 | A 的会话也被清掉 |

**重点**：4.2 和 4.4 是"多国 SIM 同时开 VoWiFi"这个核心目标的验收点。

---

## 5. 语音 / 短信路由（每卡独立策略）

| # | 操作 | 期望结果 |
|---|------|----------|
| 5.1 | 给 A 设 SMS 路径为「仅 VoLTE」，B 设为「仅 VoWiFi」 | 各自按各自顺序发；互不影响 |
| 5.2 | 新建/首次发现一条线路，不修改策略 | 使用该线路自己的默认策略（SMS：VoWiFi→VoLTE→CS；语音：VoWiFi→VoLTE） |
| 5.3 | 修改 A 的策略后查看 B | B 的策略不变；当前不存在全局策略继承层 |
| 5.4 | A 正在通话中，用 B 拨号 | **B 能拨出**（以前会被"已有通话进行中"挡住） |
| 5.5 | 指定 `line_id` 发短信 | 走该线路；不再因为"VoWiFi 只有一个运行时"而跳过 VoWiFi 路径 |

---

## 6. eSIM（多读卡器）

| # | 操作 | 期望结果 |
|---|------|----------|
| 6.1 | 带 `line_id` 读 eUICC 信息 | 读的是该线路读卡器上的 eUICC（EID 不同） |
| 6.2 | 带 `line_id` 列 profile | 列的是该卡的 profile |
| 6.3 | 带 `line_id` 下载/启用/改名/删除 profile | 只作用于该卡 |
| 6.4 | 调用不含线路资源段的旧 eSIM URL | 返回 404；当前 eUICC/Profile 操作必须显式使用线路级 URL |
| 6.5 | 将线路 eSIM 控制设为自动/启用/禁用 | 自动按探测结果开放；启用可覆盖探测；禁用时拒绝 lpac 操作 |

> `LPAC_APDU_QMI_DEVICE` 不再硬编码 `/dev/wwan0qmi0`，
> 改为从线路注册表解析。6.1 若两条线路读到同一个 EID，说明解析没生效。

---

## 6.5 流量统计（代理出口）

| # | 操作 | 期望结果 |
|---|------|----------|
| 6.5.1 | 开线路A 数据 + 代理，通过代理下载一个文件 | 「流量用量」实时增长；**上行远小于下行** |
| 6.5.2 | 通过代理上传 | 上行增长 |
| 6.5.3 | 长连接跑着不断开 | 数字持续变化，不是等断开才跳（边传边计数） |
| 6.5.4 | 同时用 A、B 两条线路的代理 | 各自独立计数，不串 |
| 6.5.5 | 重启 simadmin | 累计值保留（最多丢最后 30 秒） |
| 6.5.6 | 点「清零」 | 只清该线路，另一条不受影响 |
| 6.5.7 | 没走过流量的卡 | 显示「这张卡还没有走过流量」 |

## 6.6 全局飞行模式 / 漫游接口已删除

| # | 操作 | 期望结果 |
|---|------|----------|
| 6.6.1 | `curl -X POST .../api/airplane-mode` | **404**（接口已删除） |
| 6.6.2 | `curl .../api/roaming` | **404** |
| 6.6.3 | 前端各页面 | 没有任何地方还在调这两个接口（看浏览器 Network） |
| 6.6.4 | 线路B 开飞行模式后等 1 分钟 | watchdog **不会**把它重新拉起来 |

## 6.7 运营商 Profile catalog 与本地覆盖

| # | 操作 | 期望结果 |
|---|------|----------|
| 6.7.1 | 使用受支持且 `sealed=1` 的 schema v7 catalog 启动 | 服务启动成功，日志打印 release ID 与生成时间 |
| 6.7.2 | 分别用缺失、未封存、旧 schema catalog 启动 | 服务明确拒绝启动，不静默使用 Rust 内置或动态推导配置 |
| 6.7.3 | 查看运营商 Profile 列表 | 内容与当前 catalog release 一致，来源标为 `carrier_catalog:<release_id>` |
| 6.7.4 | 查询 catalog 不包含的 PLMN（如 46001） | 返回未匹配/不可用，不动态生成可拨号 Profile |
| 6.7.5 | 编辑 catalog 中某条 profile 的 ePDG 主机并保存，重连 VoWiFi | 本地覆盖优先，运行时立即使用新值 |
| 6.7.6 | 删除本地覆盖 | 恢复只读 catalog 基线，catalog 文件本身未被修改 |
| 6.7.7 | 改 `Expires`，抓 REGISTER 包 | `Expires:` 头使用新值 |
| 6.7.8 | 改「接入网类型」 | `P-Access-Network-Info` 头跟着变 |
| 6.7.9 | sec-agree 设为 `disabled` | REGISTER 不再携带 Security-Client/Verify |
| 6.7.10 | DNS 填两个，第一个不可达 | 日志显示继续尝试候选，最终使用第二个解析成功 |
| 6.7.11 | 导入 AOSP apns-conf.xml / CarrierConfig / IPCC XML plist | 只叠加可验证事实；catalog 中没有基线的 PLMN 被跳过 |
| 6.7.12 | 编辑美国/非美国 profile | E911 元数据可展示，但不应伪装为完整紧急呼叫实现或阻塞普通保存 |
| 6.7.13 | 升级前设备上有 `vowifi-profiles.conf` | 启动后自动迁移进数据库，文件被改名为 `.conf.migrated`；日志有 `Migrated vowifi-profiles.conf` |
| 6.7.14 | 迁移后再重启 | 不重复迁移（文件已改名） |
| 6.7.15 | 迁移后看该运营商 profile | 旧文件字段成为本地覆盖，缺失字段来自 catalog 基线而不是动态推导 |
| 6.7.16 | 改一条 profile 后**不重启**，直接连 VoWiFi | 用的是新值（保存时会立即推送给运行时匹配器） |

> 6.7.16 很关键：如果改了没生效，说明 `ProfileStore::publish()` 没被调用。

> carrier catalog 与 `data.db` 的本地覆盖是两种不同制品。升级 catalog 后需回归本地覆盖
> 仍能叠加；不要用测试 SQLite 覆盖设备的 `data.db`。

## 7. 热插拔 / 重启 / 持久化

| # | 操作 | 期望结果 |
|---|------|----------|
| 7.1 | 拔掉 B 卡再插回 | B 恢复为同一个 `line_id`，之前的设置还在 |
| 7.2 | 重启 simadmin | 两条线路的 VoLTE/VoWiFi/APN/代理/路径策略配置全部保留 |
| 7.3 | 重启后**不自动全局连接** | 只有各线路自己 `enabled=true` 的才会连（不再有全局开机自连） |
| 7.4 | 重启设备 | udev 规则生效，DATA6 端点自动出现 |

---

## 8. 回归（确认没把单卡场景改坏）

| # | 操作 | 期望结果 |
|---|------|----------|
| 8.1 | 只插一张卡，使用线路详情页 | 所有线路级功能正常，不要求存在第二张卡 |
| 8.2 | 调用已删除的全局单 modem API | 返回 404；前端和 Bruno 不应再依赖隐式“第一条线路” |
| 8.3 | 旧的 config.json 直接启动 | 能正常加载（新字段都是 `Option`/`default`） |

---

## 已知未解决 / 需要真机才能推进

1. **ISIM 身份文件读取**（待办 #9）
   当前已能发现 ISIM AID，但 IMPI/IMPU 仍主要按 IMSI 推导。完整读取需要
   SELECT EF_IMPI/EF_IMPU/EF_DOMAIN/EF_IST + READ BINARY。
   → 需要一张带 ISIM 应用的卡才能开发和验证。

2. **UDP Relay 模式**（待办 #17）
   已在 UI 标记为不可用，建议直接用标准 SOCKS5 自建替代。

3. **运营商专有行为覆盖度**
   catalog 已接入多项 REGISTER、重试、keepalive 和 Contact 配置，但不同运营商仍需真机
   样本验证；`partial` 条目不得当作 `ready` 使用。

---

## 出问题时先看这几个地方

- `journalctl -u simadmin -f | grep -i "line_id"` —— 确认操作打到了哪条线路
- `/api/volte/lines/{line_id}` 的 `attempt` 数组 —— 每次连接尝试的结构化原因
- `/api/vowifi/lines/{line_id}/events?trace_id=...` —— IKE/ESP/REGISTER 各阶段
- `ip link` / `ip route` / `ip rule` —— TUN 与路由是否互相踩
- `dmesg | tail` —— 内核模块绑定/解绑、qmi 端口掉线
