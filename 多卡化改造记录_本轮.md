# 多卡 / 多读卡器改造记录（本轮）

目标：把 SimAdmin 从"单基带 + 全局状态"改成**每条线路(line)完全独立**，
支持多 SIM / 多读卡器设备，每张卡有自己的设置、自己的运行时、自己的出口。

真机验证项见 `真机测试清单.md`。

---

## 一、VoWiFi 运行时 per-line 化（本轮主线）

以前只有一个全局 `VowifiRuntime`，一个 TUN 网卡，一套 IMS 会话缓存，
所以第二张卡开 VoWiFi 会把第一张卡顶掉。

**做了什么**

- `VowifiRuntime` 带上 `line_id`；`LineRuntime` 各自持有一个
  （`VowifiRuntime::for_line`），并各自持有 `vowifi_connect_lock`
- 四个 IMS 缓存（register ready / security verify / TCP channel /
  register success variant）由单槽改为 `HashMap<line_id, _>`
- TUN 网关由单槽改为 `HashMap<line_id, _>`，网卡名按线路派生
  （`tun_name_for_line`，控制在 `IFNAMSIZ` 15 字节内）
- ePDG / DNS / 代理覆盖配置按 `line_id` 存取
- `line_id` 贯穿整条链路：IKE → ESP → IMS REGISTER → SMS → Voice，
  包括 `build_live_register_auth_material` 里的 **EAP-AKA**
  （这是最严重的一处：以前永远用全局读卡器做 AKA，多卡时网络会拒绝）
- `VowifiRuntime::refresh_identity` 现在从线路注册表解析自己的 modem，
  不再"取第一个 modem"，所以线路 B 不会匹配到线路 A 的运营商 profile
- handlers 引入 `VowifiScope`：显式 `line_id` 优先，省略则落到主线路；
  **删除了 `is_primary` 连接闸**，任何线路都能自己连
- `clear_live_runtime_for_line` 让断开某条线路不再清掉其他线路的会话

**结果**：多张不同国家的 SIM 可以同时在线跑 VoWiFi，各走各的代理，
互不影响主程序。

---

## 二、代理 / DNS

- 删除 `ConnectUdpMasque` 模式。理由写在代码里：Cloudflare 的 MASQUE
  说的是 Connect-IP（RFC 9484）而不是 Connect-UDP（RFC 9298），还要账号
  和设备注册，且只能出到 Cloudflare 自己的网络，到不了运营商 ePDG
- 新增 `socks5.rs`：完整的 SOCKS5 UDP ASSOCIATE（RFC 1928 §4/§7 +
  RFC 1929 用户名密码认证）。关键是 `normalize_relay_addr()` —— 把
  服务端返回的通配地址 `0.0.0.0`/`::` 改写成代理自身 IP，这是能配合
  mihomo / sing-box 的前提
- 控制连接在整个会话期间保持（RFC 1928 §7 要求），带 `is_control_alive()`
- DNS 跟随代理：配了代理就走代理解析，没配就只指定 DNS 解析服务器直连
- 每条线路可以配自己的代理，互不影响
- UDP Relay 模式暂缓（UI 已标为不可用），建议直接自建标准 SOCKS5

---

## 三、消除全局基带路径

以前这些操作**一律打到第一个基带**，但 UI 显示的是"正在配置线路 N"。
这是本轮最影响用户的一类 bug。

**后端**：为下列能力加了 `*_for_modem` / `*_on_modem` 版本，并**删掉了
原来的全局包装**，这样没有任何代码路径能再退回"随便挑一个基带"：

| 能力 | 新函数 |
|------|--------|
| 小区信息 | `get_cells_data_for_modem` |
| 射频模式 | `get_radio_mode_for_modem` / `set_radio_mode_for_modem` |
| 频段锁定 | `get_band_lock_status_for_modem` / `set_band_lock_for_modem` |
| 运营商 | `get_operators_list_for_modem` / `scan_operators_for_modem` / `register_operator_for_modem` |
| APN | `list_apn_contexts_for_modem` |
| 通话 | `list_current_calls_for_modem` / `make_call_on_modem` |

**handlers**：新增 `resolve_modem_path(app, line_id)`。指定了 `line_id`
但线路不存在/不在位时**直接报错**，不再静默回落到主线路 —— 静默回落
正是"UI 说在配置线路 N、实际写到线路 1"的根因。

**小区锁定**：`AppState::cell_lock` 由单个 store 改为
`HashMap<line_id, CellLockStore>`。

**通话**：拨号前的"已有通话进行中"检查以前查的是全局通话列表，导致
线路 A 在通话时线路 B 根本拨不出去。现在只查该基带自己的通话。

**eSIM**：`LPAC_APDU_QMI_DEVICE` 不再硬编码 `/dev/wwan0qmi0`，
改为从与 VoWiFi/VoLTE 共用的线路注册表解析读卡器；
eUICC 信息 / profile 列表 / 下载 / 启用 / 改名 / 删除都接受 `line_id`。

**前端**：`LineCellularSettings` 以前只收一个装饰性的 `lineLabel`，
现在必须传 `lineId`，并透传到每个读写调用；
`api/current.ts` 加了 `lineScopeQuery()` 统一拼 `?line_id=`。

---

## 四、每卡独立的配置项（新增）

`LineProfileConfig` 新增三个**可选**覆盖字段，`None` 表示继承全局，
所以旧配置文件和单卡设备行为完全不变：

- `sms_path: Option<SmsPathPolicy>` —— 每卡自己的短信路径优先级
- `voice_path: Option<VoicePathPolicy>` —— 每卡自己的语音路径优先级
- `apn: Option<ApnConfig>` —— 每卡自己的 APN

配套 `get_line_*` / `set_line_*` 访问器，以及一个通用的
`update_line_profile()` 辅助函数（原本每个 setter 都在重复
find-or-insert / sort / save 这套动作）。

发短信时按该线路的策略走，所以"只有 VoLTE 能用的卡"和
"只有 VoWiFi 能用的卡"可以共存在一台设备里。
也因此删掉了原来那句"指定了 line_id 就跳过 VoWiFi 路径"的补丁。

---

## 五、全局与 per-line 的一致性修复

- 全局飞行模式接口驱动的是主基带，现在会把结果**同步写回主线路的
  profile**。以前不写，导致线路页一直显示旧值、开关和实际射频状态打架
- 全局漫游接口同理

---

## 六、VoLTE（承接上一轮）

- 删除全局连接闸，纯 per-line
- 连接尝试结构化字段：`at_cid` / `qmi_device` / `bearer_path` /
  `interface` / `pcscf`
- IP 家族策略统一到 `ImsConnectionPlan`：默认双栈，网络明确要求某个
  家族时直接切过去，没有明确要求时先 IPv4 再 IPv6
- per-baseband secondary QMI 端点（DATA6），IMS bearer 不再和
  ModemManager 抢 `/dev/wwan0qmi0`
- 数据连接仅做代理出口，不改系统默认路由

---

## 七、状态

- `cargo build` / `cargo clippy --all-targets`：干净
- `cargo test`：621 通过
- `tsc --noEmit`：干净

## 八、仍未解决

见 `真机测试清单.md` 末尾"已知未解决"一节：
同运营商双卡 P-CSCF 路由冲突、ISIM 身份读取、NetworkManager
`multi-connect`、进程级 `DBUS_LOCK`、UDP Relay。
这几项都需要真机复现或特定 SIM 才能推进。
