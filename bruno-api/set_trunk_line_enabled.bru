meta {
  name: set_trunk_line_enabled
  type: http
  seq: 164
}

post {
  url: {{base_url}}/api/trunk/lines/{{line_id}}/enabled
  body: json
  auth: inherit
}

body:json {
  {
    "enabled": true
  }
}

settings {
  encodeUrl: true
}
