meta {
  name: retry_volte_line
  type: http
  seq: 155
}

post {
  url: {{base_url}}/api/volte/lines/{{line_id}}/retry
  body: json
  auth: inherit
}

body:json {
  {}
}

settings {
  encodeUrl: true
}
