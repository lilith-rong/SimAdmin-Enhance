meta {
  name: Set Call Forwarding
  type: http
  seq: 212
}

post {
  url: {{base_url}}/api/modem/lines/{{line_id}}/calls/forwarding
  body: json
  auth: inherit
}

body:json {
  {
    "forward_type": "unconditional",
    "number": "10086"
  }
}

settings {
  encodeUrl: true
}
