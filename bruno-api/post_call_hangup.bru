meta {
  name: Hangup Call
  type: http
  seq: 102
}

post {
  url: {{base_url}}/api/modem/lines/{{line_id}}/calls/hangup
  body: json
  auth: inherit
}

body:json {
  {
    "path": "/ril_0/voicecall01"
  }
}

settings {
  encodeUrl: true
}

