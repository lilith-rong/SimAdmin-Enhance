meta {
  name: Set Call Settings
  type: http
  seq: 214
}

post {
  url: {{base_url}}/api/modem/lines/{{line_id}}/calls/settings
  body: json
  auth: inherit
}

body:json {
  {
    "property": "HideCallerId",
    "value": "default"
  }
}

settings {
  encodeUrl: true
}

