meta {
  name: set_data_status_disable
  type: http
  seq: 10
}

post {
  url: {{base_url}}/api/modem/lines/{{line_id}}/data
  body: json
  auth: inherit
}

body:json {
  {
    "active": false
  }
}

settings {
  encodeUrl: true
}

